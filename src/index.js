import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App"; // importing the App module. Also mentioning the folder name since index.js and App.jsx reside in different folders

ReactDOM.render(<App />, document.getElementById("root"));
